# لوحة تحكم أثر - Athar Admin Dashboard

لوحة تحكم إدارية لتطبيق أثر مبنية باستخدام Vue 3 + TypeScript + Vite

## المميزات

- 📊 لوحة تحكم شاملة مع إحصائيات مفصلة
- 👥 إدارة المستخدمين (عرض، حظر، حذف)
- 📝 إدارة المنشورات (عرض، حذف، فلترة)
- 💬 إدارة التعليقات
- 🔔 إرسال إشعارات للمستخدمين
- ⚙️ إعدادات التطبيق
- 🔐 نظام مصادقة آمن
- 📱 تصميم متجاوب

## التثبيت

```bash
cd dashboard
npm install
# أو
yarn install
```

## التشغيل

### وضع التطوير

```bash
npm run dev
# أو
yarn dev
```

سيعمل التطبيق على `http://localhost:5173`

### البناء للإنتاج

```bash
npm run build
# أو
yarn build
```

### معاينة البناء

```bash
npm run preview
# أو
yarn preview
```

## الإعدادات

قم بإنشاء ملف `.env` في مجلد `dashboard` وأضف:

```
VITE_API_URL=http://localhost:3000/api
```

## تسجيل الدخول

للوصول إلى لوحة التحكم، يجب أن يكون لديك حساب مدير (role = 'admin').

يمكنك إنشاء حساب مدير من خلال تحديث قاعدة البيانات مباشرة:

```sql
UPDATE users SET role = 'admin' WHERE phone = '07701234567';
```

## البنية

```
dashboard/
├── src/
│   ├── components/      # المكونات القابلة لإعادة الاستخدام
│   ├── layouts/         # تخطيطات الصفحات
│   ├── views/           # صفحات التطبيق
│   ├── stores/          # Pinia stores
│   ├── router/          # إعدادات Vue Router
│   ├── App.vue          # المكون الرئيسي
│   ├── main.ts          # نقطة الدخول
│   └── style.css        # الأنماط العامة
├── public/              # الملفات الثابتة
├── index.html           # HTML الرئيسي
├── vite.config.ts       # إعدادات Vite
├── tailwind.config.js   # إعدادات Tailwind
└── package.json
```

## الصفحات

- `/` - الصفحة الرئيسية (الإحصائيات)
- `/users` - إدارة المستخدمين
- `/posts` - إدارة المنشورات
- `/comments` - إدارة التعليقات
- `/notifications` - إرسال الإشعارات
- `/settings` - الإعدادات
- `/login` - تسجيل الدخول

## التقنيات المستخدمة

- Vue 3 (Composition API)
- TypeScript
- Vite
- Vue Router
- Pinia (State Management)
- Axios (HTTP Client)
- Tailwind CSS
- Chart.js + vue-chartjs

## الأمان

- جميع الطلبات تتطلب مصادقة
- التحقق من دور المستخدم (admin فقط)
- Token-based authentication
- حماية من CSRF

## الدعم

للمساعدة أو الإبلاغ عن مشاكل، يرجى فتح issue في المستودع.