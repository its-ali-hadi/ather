# 🚀 دليل الإنتاج - منصة أثر

## ✅ التحضيرات المكتملة

تم تجهيز المشروع بالكامل للإنتاج مع التحسينات التالية:

### 1. Frontend (React Native + Expo)

#### ✅ المميزات المكتملة
- ✅ نظام مصادقة كامل (OTP عبر WhatsApp)
- ✅ وضع الضيف للتصفح بدون تسجيل
- ✅ واجهة مستخدم احترافية مع دعم الوضع الداكن
- ✅ Safe Area للأجهزة المختلفة
- ✅ شريط تنقل سفلي محسّن
- ✅ صفحات كاملة (الرئيسية، استكشف، إنشاء، خاص، الملف الشخصي)
- ✅ إدارة الحالة مع Context API
- ✅ معالجة الأخطاء العامة
- ✅ دعم الإشعارات Push

#### 🎨 التصميم
- نظام ألوان متناسق (بني فاتح/ذهبي)
- خطوط عربية احترافية (Cairo & Tajawal)
- رسوم متحركة سلسة مع Reanimated
- تدرجات لونية وظلال احترافية

### 2. Backend (Node.js + Express + MySQL)

#### ✅ المميزات المكتملة
- ✅ API RESTful كامل
- ✅ نظام مصادقة JWT
- ✅ OTP عبر WhatsApp (otp.dev)
- ✅ إدارة المستخدمين والمنشورات
- ✅ نظام الإعجابات والتعليقات
- ✅ نظام المفضلات
- ✅ نظام المتابعة
- ✅ نظام الإشعارات
- ✅ لوحة تحكم الإدارة
- ✅ Seed Data تلقائي

#### 🔒 الأمان
- Helmet.js للحماية
- CORS محدد
- JWT آمن
- تشفير كلمات المرور (bcrypt)
- Validation للمدخلات

### 3. قاعدة البيانات

#### ✅ الجداول المكتملة
- users (المستخدمين)
- posts (المنشورات)
- comments (التعليقات)
- likes (الإعجابات)
- favorites (المفضلات)
- follows (المتابعات)
- notifications (الإشعارات)

## 📋 خطوات التشغيل

### Backend

```bash
cd backend

# تثبيت المكتبات
npm install

# إعداد ملف البيئة
cp .env.example .env
# قم بتعديل .env بالمعلومات الصحيحة

# إنشاء قاعدة البيانات
npm run init-db

# تشغيل السيرفر (سيتم seed البيانات تلقائياً)
npm start

# أو للتطوير مع auto-reload
npm run dev
```

### Frontend

```bash
# تثبيت المكتبات
npm install

# تشغيل التطبيق
npm start

# للأندرويد
npm run android

# للـ iOS
npm run ios

# للويب
npm run web
```

## 🔐 حسابات الاختبار

بعد تشغيل seed data، ستتوفر الحسابات التالية:

### حساب مستخدم عادي
- **الهاتف:** 07701234567
- **كلمة المرور:** password123

### حساب المدير
- **الهاتف:** 07789012345
- **كلمة المرور:** password123

## 🌱 Seed Data

يتم تحميل البيانات التجريبية تلقائياً عند تشغيل السيرفر إذا كانت `AUTO_SEED=true` في ملف `.env`.

البيانات المحملة:
- 9 مستخدمين (8 عاديين + 1 مدير)
- 12 منشور
- 15 إعجاب
- 8 تعليقات
- 10 متابعات

لتعطيل التحميل التلقائي، غيّر `AUTO_SEED=false` في `.env`.

## 🚀 النشر للإنتاج

### Backend

1. **إعداد السيرفر**
   ```bash
   # على السيرفر
   git clone <repository-url>
   cd backend
   npm install --production
   ```

2. **إعداد قاعدة البيانات**
   ```bash
   npm run init-db
   ```

3. **إعداد متغيرات البيئة**
   - قم بإنشاء ملف `.env` بالمعلومات الصحيحة
   - غيّر `NODE_ENV=production`
   - غيّر `AUTO_SEED=false` (للإنتاج)
   - استخدم JWT_SECRET قوي
   - حدد FRONTEND_URL الصحيح

4. **تشغيل السيرفر**
   ```bash
   # استخدم PM2 للإنتاج
   npm install -g pm2
   pm2 start src/server.js --name athar-api
   pm2 save
   pm2 startup
   ```

### Frontend

1. **إعداد ملف البيئة**
   ```bash
   # في .env.local
   EXPO_PUBLIC_API_URL=https://your-api-domain.com/api
   ```

2. **بناء التطبيق**
   ```bash
   # للأندرويد
   eas build --platform android

   # للـ iOS
   eas build --platform ios
   ```

## 📱 التغييرات الرئيسية

### 1. شريط التنقل
- ✅ تم تغيير "الإشعارات" إلى "خاص"
- ✅ إضافة Safe Area للأجهزة المختلفة
- ✅ ارتفاع محسّن للـ iOS (88px) والأندرويد (60px)

### 2. صفحة الضيف
- ✅ إضافة Scroll للمحتوى
- ✅ عرض 6 مميزات بدلاً من 4
- ✅ إضافة صندوق معلومات في الأسفل

### 3. نظام المصادقة
- ✅ إزالة البريد الإلكتروني وكلمة المرور من التسجيل
- ✅ الاعتماد فقط على رقم الهاتف + OTP
- ✅ تسجيل الدخول بـ OTP فقط

### 4. Seed Data
- ✅ نقل جميع البيانات التجريبية للـ Backend
- ✅ إزالة seed-data.json من Frontend
- ✅ تحميل تلقائي عند بدء السيرفر

## 🔧 المتطلبات

### Backend
- Node.js 14+
- MySQL 5.7+
- npm أو yarn

### Frontend
- Node.js 14+
- Expo CLI
- npm أو yarn

## 📝 ملاحظات مهمة

1. **OTP Configuration**: تأكد من إعداد حساب otp.dev وإضافة المعلومات في `.env`

2. **Database**: قم بإنشاء قاعدة البيانات قبل تشغيل السيرفر

3. **CORS**: تأكد من إضافة domain الـ Frontend في إعدادات CORS

4. **JWT Secret**: استخدم مفتاح قوي وعشوائي في الإنتاج

5. **Auto Seed**: عطّل AUTO_SEED في الإنتاج بعد أول تشغيل

## 🐛 استكشاف الأخطاء

### مشكلة الاتصال بقاعدة البيانات
```bash
# تحقق من إعدادات MySQL
mysql -u root -p
CREATE DATABASE athar_db;
```

### مشكلة OTP
- تحقق من صحة معلومات otp.dev في `.env`
- تأكد من تفعيل WhatsApp في حساب otp.dev

### مشكلة CORS
- تأكد من إضافة URL الـ Frontend في `FRONTEND_URL`
- تحقق من إعدادات CORS في `server.js`

## 📞 الدعم

للمساعدة أو الإبلاغ عن مشاكل، يرجى فتح issue في المستودع.

## 📄 الترخيص

هذا المشروع مرخص تحت [اسم الترخيص].

---

**تم التجهيز بواسطة:** فريق تطوير منصة أثر
**التاريخ:** 2024
**الإصدار:** 1.0.0 (Production Ready)