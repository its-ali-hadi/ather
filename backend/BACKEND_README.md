# 🚀 Athar Backend API

## 📋 نظرة عامة

Backend API لمنصة أثر - منصة لمشاركة الأفكار والمحتوى.

---

## 🛠️ التقنيات المستخدمة

- **Node.js** + **Express.js**
- **MySQL** (قاعدة البيانات)
- **JWT** (المصادقة)
- **OTP.dev** (التحقق عبر WhatsApp)
- **AWS S3** (تخزين الملفات)

---

## 📦 التثبيت

```bash
# 1. تثبيت Dependencies
npm install

# 2. إعداد ملف .env
cp .env.example .env
# ثم قم بتعديل القيم في .env

# 3. إنشاء قاعدة البيانات
npm run init-db

# 4. (اختياري) إضافة بيانات تجريبية
npm run seed

# 5. تشغيل السيرفر
npm start
```

---

## 🗄️ بنية قاعدة البيانات

### **جداول قاعدة البيانات:**

#### 1. **users**
- `id` - معرف المستخدم
- `phone` - رقم الهاتف (فريد)
- `name` - الاسم
- `email` - البريد الإلكتروني
- `password` - كلمة المرور (مشفرة)
- `bio` - نبذة عن المستخدم
- `profile_image` - صورة الملف الشخصي
- `push_token` - رمز الإشعارات
- `is_verified` - حالة التحقق
- `is_banned` - حالة الحظر ✨ **جديد**
- `ban_reason` - سبب الحظر ✨ **جديد**
- `role` - الدور (user/admin)

#### 2. **posts**
- `id` - معرف المنشور
- `user_id` - معرف المستخدم
- `type` - نوع المنشور (text/image/video/link)
- `title` - العنوان
- `content` - المحتوى
- `media_url` - رابط الوسائط
- `link_url` - رابط خارجي
- `category` - التصنيف
- `is_archived` - حالة الأرشفة
- `is_private` - منشور خاص/عام ✨ **جديد**
- `is_featured` - منشور مميز ✨ **جديد**
- `views_count` - عدد المشاهدات

#### 3. **comments**
- `id` - معرف التعليق
- `post_id` - معرف المنشور
- `user_id` - معرف المستخدم
- `content` - المحتوى
- `parent_id` - معرف التعليق الأب (للردود)

#### 4. **likes**
- `id` - معرف الإعجاب
- `post_id` - معرف المنشور
- `user_id` - معرف المستخدم

#### 5. **favorites**
- `id` - معرف المفضلة
- `post_id` - معرف المنشور
- `user_id` - معرف المستخدم

#### 6. **follows**
- `id` - معرف المتابعة
- `follower_id` - معرف المتابِع
- `followed_id` - معرف المتابَع

#### 7. **notifications**
- `id` - معرف الإشعار
- `user_id` - معرف المستخدم
- `type` - نوع الإشعار
- `content` - المحتوى
- `related_id` - معرف العنصر المرتبط
- `is_read` - حالة القراءة

---

## 🔧 Scripts المتاحة

```bash
# تشغيل السيرفر
npm start

# تشغيل السيرفر مع التحديث التلقائي (Development)
npm run dev

# إنشاء قاعدة البيانات (يشمل جميع الحقول)
npm run init-db

# إضافة بيانات تجريبية
npm run seed

# إنشاء مستخدم تجريبي
npm run create-test-user
```

---

## 📝 ملاحظات مهمة

### ✅ **لا توجد ملفات Migration منفصلة**
- جميع الحقول موجودة في `initDatabase.js`
- لا حاجة لتشغيل migrations منفصلة
- فقط قم بتشغيل `npm run init-db` لإنشاء قاعدة البيانات كاملة

### ✅ **الحقول الجديدة المضافة:**
1. **في جدول users:**
   - `is_banned` - لحظر المستخدمين
   - `ban_reason` - لتوضيح سبب الحظر

2. **في جدول posts:**
   - `is_private` - للمنشورات الخاصة/العامة
   - `is_featured` - للمنشورات المميزة

---

## 🔐 المصادقة

### **تسجيل الدخول:**
1. المستخدم يدخل رقم الهاتف
2. يتم إرسال OTP عبر WhatsApp
3. المستخدم يدخل OTP
4. يتم إنشاء JWT Token

### **الحماية:**
- جميع المسارات المحمية تحتاج JWT Token
- Token يتم إرساله في Header: `Authorization: Bearer <token>`

---

## 📡 API Endpoints

### **Auth:**
- `POST /api/auth/send-otp` - إرسال OTP
- `POST /api/auth/verify-otp` - التحقق من OTP
- `POST /api/auth/register` - التسجيل
- `POST /api/auth/login` - تسجيل الدخول

### **Posts:**
- `GET /api/posts` - جميع المنشورات العامة
- `GET /api/posts/my/private` - المنشورات الخاصة للمستخدم ✨ **جديد**
- `GET /api/posts/:id` - منشور واحد
- `POST /api/posts` - إنشاء منشور
- `PUT /api/posts/:id` - تحديث منشور
- `DELETE /api/posts/:id` - حذف منشور

### **Users:**
- `GET /api/users/profile` - الملف الشخصي
- `PUT /api/users/profile` - تحديث الملف الشخصي
- `GET /api/users/:id` - ملف مستخدم آخر

### **Comments:**
- `GET /api/comments/:postId` - تعليقات منشور
- `POST /api/comments` - إضافة تعليق
- `DELETE /api/comments/:id` - حذف تعليق

### **Likes:**
- `POST /api/likes/:postId` - إضافة/إزالة إعجاب

### **Favorites:**
- `GET /api/favorites` - المفضلات
- `POST /api/favorites/:postId` - إضافة/إزالة من المفضلات

---

## 🚀 النشر على VPS

راجع ملف `FINAL_PRODUCTION_STATUS.md` للحصول على دليل كامل للنشر.

---

## 📞 الدعم

للمساعدة أو الاستفسارات، راجع التوثيق الكامل في المشروع.

---

**آخر تحديث:** 2024  
**الإصدار:** 1.0.0
