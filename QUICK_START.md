# 🚀 دليل البدء السريع - منصة أثر

## ⚡ البدء في 5 دقائق

### 1. تثبيت المتطلبات

تأكد من تثبيت:
- Node.js 14+ ([تحميل](https://nodejs.org/))
- MySQL 5.7+ ([تحميل](https://dev.mysql.com/downloads/mysql/))
- Expo CLI: `npm install -g expo-cli`

### 2. إعداد قاعدة البيانات

```bash
# افتح MySQL
mysql -u root -p

# أنشئ قاعدة البيانات
CREATE DATABASE athar_db;
exit;
```

### 3. إعداد Backend

```bash
# انتقل لمجلد Backend
cd backend

# تثبيت المكتبات
npm install

# انسخ ملف البيئة
cp .env.example .env

# عدّل .env بمعلوماتك:
# DB_PASSWORD=كلمة_مرور_MySQL
# OTP_DEV_APP_ID=معرف_تطبيق_otp.dev
# (اترك AUTO_SEED=true لأول مرة)

# أنشئ الجداول
npm run init-db

# شغّل السيرفر
npm start
```

سترى رسالة نجاح مع معلومات حسابات الاختبار.

### 4. إعداد Frontend

```bash
# في نافذة terminal جديدة، ارجع للمجلد الرئيسي
cd ..

# تثبيت المكتبات
npm install

# شغّل التطبيق
npm start
```

### 5. افتح التطبيق

- اضغط `w` للويب
- اضغط `a` للأندرويد (مع محاكي مفتوح)
- اضغط `i` للـ iOS (Mac فقط)
- امسح QR code بتطبيق Expo Go على هاتفك

## 🎯 تسجيل الدخول

استخدم أحد حسابات الاختبار:

**مستخدم عادي:**
- الهاتف: `07701234567`
- كلمة المرور: `password123`

**مدير:**
- الهاتف: `07789012345`
- كلمة المرور: `password123`

## 🔧 حل المشاكل الشائعة

### مشكلة الاتصال بقاعدة البيانات
```bash
# تحقق من تشغيل MySQL
mysql -u root -p

# تحقق من وجود قاعدة البيانات
SHOW DATABASES;
```

### مشكلة المنافذ
```bash
# إذا كان المنفذ 3000 مستخدم
# غيّر PORT في backend/.env
PORT=3001
```

### مشكلة Expo
```bash
# امسح الـ cache
expo start -c
```

## 📱 الميزات المتاحة

بعد تسجيل الدخول، يمكنك:
- ✅ تصفح المنشورات
- ✅ إنشاء منشورات جديدة
- ✅ الإعجاب والتعليق
- ✅ حفظ المنشورات
- ✅ متابعة المستخدمين
- ✅ عرض الإشعارات
- ✅ تعديل الملف الشخصي

## 🌱 إعادة تحميل البيانات

```bash
cd backend

# احذف جميع البيانات وأعد التحميل
mysql -u root -p athar_db < reset.sql
npm run seed
```

## 📚 المزيد من التوثيق

- [دليل الإنتاج الكامل](./PRODUCTION_READY.md)
- [توثيق API](./backend/API_DOCUMENTATION.md)
- [دليل Backend](./backend/BACKEND_README.md)

## 💡 نصائح

1. **للتطوير**: اترك `AUTO_SEED=true` في `.env`
2. **للإنتاج**: غيّر `AUTO_SEED=false` بعد أول تشغيل
3. **للاختبار**: استخدم حسابات الاختبار المذكورة أعلاه
4. **للتصفح**: استخدم وضع الضيف من شاشة تسجيل الدخول

## 🆘 تحتاج مساعدة؟

- افتح issue في المستودع
- راجع [التوثيق الكامل](./README.md)
- تحقق من [الأسئلة الشائعة](./FAQ.md)

---

**مستعد؟ ابدأ الآن! 🚀**